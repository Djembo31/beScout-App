---
paths:
  - ".github/**"
  - "scripts/**"
  - ".husky/**"
  - "next.config.mjs"
  - "vercel.json"
  - "*.config.ts"
  - "*.config.mjs"
---

# Errors: Infra — Navigator

Stand: 2026-06-23 · **Slice 353:** Dieser Navigator (always-loaded bei Infra-Edit: `.github/`, `scripts/`, `.husky/`, `next.config.mjs`, `vercel.json`, `*.config.*`) trägt je Pattern die **ACTIONABLE Regel inline**. Der volle Detail (Root-Cause, Code-Blöcke, Audit-greps, Reference) liegt **on-demand** in `errors-infra-detail.md`. Siehe auch `performance.md`.

> **Nutzung:** Jede Zeile ist die Guardrail — danach handeln. Trifft ein Pattern wirklich zu (oder brauchst du Code/Audit-grep/SQL) → `.claude/rules/errors-infra-detail.md` öffnen, Abschnitt « *Heading* » lesen. Pattern-Namen unten = exakte Detail-Headings.

## Build / Deploy

- **jsdom 28 + pnpm Hoisting — Silent Broken Tests** (S268b): jsdom 28 zieht `@asamuzakjp/css-color` → `@csstools/css-calc`, das pnpm nicht symlinkt → ALLE jsdom-vitest-Tests silent broken (`Test Files: no tests`). Fix: `.npmrc` Root → `public-hoist-pattern[]=@csstools/*` + `CI=true pnpm install`. Bei jedem jsdom-Major-Bump 1× Smoke-Test laufen lassen.
- **tsconfig excludes scripts** (S079): `include:["**/*.ts"]` zieht `scripts/` mit → `next build` failt (`Cannot find module 'playwright'`) obwohl `tsc` lokal clean (skipLibCheck). Fix: `exclude:[...,"scripts","tmp"]`. Nach neuen `scripts/*.ts` immer `npx next build` lokal.
- **Next.js Route-Handler Named-Exports** (S069): In `src/app/api/**/route.ts` nur HTTP-Methods + `runtime|dynamic|revalidate|fetchCache|maxDuration|generateStaticParams|config` exportieren — jeder andere Export → `next build` Type-Error (tsc fängt's NICHT). Helpers nach `src/lib/scrapers/`. Nach route.ts-Edit → `npx next build`.
- **ESLint disable-comment mit undefined rule** (S069): `// eslint-disable-next-line @typescript-eslint/...` failt wenn Plugin nicht in eslintrc → stattdessen typgerechter Cast (`as unknown as ...`).
- **Vercel Env + Module-Level + CSP**: `NEXT_PUBLIC_*` NIE als "Sensitive" markieren (wird nicht injected). KEIN `createClient()` auf Module-Level (Lazy-Init via Proxy/Getter, sonst crasht Vercel Build). CSP `img-src` aus DB ableiten, nicht raten.
- **Vercel Hobby-Tier Silent-Build-Fail bei hourly Crons** (S187, D36): Hobby = max 2 Crons, 1×/Tag; hourly-Cron (`0 * * * *`) → silent build-fail, KEINE E-Mail. Detection: nach `git push` 2-3 min via `mcp__vercel__list_deployments` Commit-SHA verifizieren; fehlt er → `vercel deploy --prod --yes` für echte Fehlermeldung. Fix: Schedule auf daily ODER Pro-Plan.
- **Bundle-Budget-Gate** (S185b): `bundle-budget.json` definiert thresholds pro Route; `pnpm run size` / CI-Gate → `scripts/check-bundle-size.ts` exit 1 bei Regression. Budget nur mit Justification anheben.
- **dynamic() rettet nur wenn KEIN anderer Pfad eager laedt** (S121): Lazy-`import()` in queryFn nützt 0 wenn anderer Codepfad das Modul eager importiert (bleibt in beiden Chunks). Vor „Seite X FLJS sinkt": `grep -rn "from.*'@/lib/services/<modul>'" src/` → ALLE Call-Sites prüfen.
- **Namespace-Import blockiert Tree-Shaking** (S120): `import * as X + X[dynamic]` = Webpack bundlet alles, `optimizePackageImports` tree-shaked nicht. Lösung: Static assets (`public/`+`<img>`) ODER Named imports ODER Factory-Map mit `React.lazy`.
- **Query-Konsolidierung ≠ LCP-Win wenn Queries parallel** (S109): N Hooks → 1 RPC eliminiert Calls, aber React Query feuert sie ohnehin parallel → LCP-Delta im Rauschen. Latenz-Win nur bei Waterfall/LCP-blocking/HTTP1. Structural Wins (Roundtrips, Konsistenz, DB-Last) bleiben echt.
- **LHCI/Lighthouse-Fallen-Sammlung** (S282b): (1) Pflicht-Verify `requestedUrl == finalDisplayedUrl` über alle Reports (auth-gated Pages werden sonst als /login gemessen). (2) Login-State NIE via `page.url()` prüfen → Session-Cookie `sb-*-auth-token`. (3) `lhci collect` ohne upload schreibt KEINE Reports → Freshness via `fetchTime`. (4) Chrome-Pfad auf `collect.chromePath`-Ebene. GHA: `include-hidden-files: true` für `.lighthouseci/`-Upload.
- **Cron-Skip-Branch ohne advance_gameweek-Aufruf → chronischer GW-Drift** (S273+276b): Jede Skip-Branch im Daily-Sync die early-returnt MUSS vor `return` prüfen ob State-Advance (`advance_gameweek`) fällig ist, sonst klebt State auf fertiger Period → rekurrenter GW-Drift. Drift-Detector-SQL (`first_open_gw > active_gameweek`) als post-Cron-Smoke. Process-Lehre: „Backlog für nächsten Slice" MUSS exakt der nächste Slice sein, nicht ein neuer Live-Bug.

## Cross-Cutting / Operational

- **Branch-Protection enforce_admins=true ist NICHT direct-push-kompatibel** (S244+248): Bei Solo-Dev direct-push → Catch-22 (`X of X required status checks expected` / `protected branch hook declined`), CI startet erst nach Push. Fix: `enforce_admins=false` + Pre-Push-Hook der Checks lokal simuliert (`CI=true pnpm exec vitest run` für Integration-Parität). Bypass nur `git push --no-verify`.
- **Grep-Audit-Scope-Gap bei Sub-Component-Scan** (S166): Top-Level-Grep verpasst embedded Modals in Cards/Tabs. Full-recursive `grep -rn "<Modal" src/` + cross-ref mit Mutation-State-Pattern (`isPending|loading|saving`). Reviewer-Agent als Scope-Gap-Catcher.
- **Removal/Migration-Slice: Broken-Ref-Grep MUSS Live-Doku-Schicht abdecken** (E0-W2c): Bei jedem Stub-/File-Removal den Broken-Ref-Grep repo-weit über Live-Doku (`MASTERPLAN.md TODO.md memory/MEMORY.md memory/session-handoff.md memory/decisions.md` + `.claude/`), NICHT nur Code/Hooks. „Read this"-Pointer = genauso Broken-Ref. Frozen Records (`worklog/specs|proofs|reviews`, `_archive`) ausschließen. Prinzip „repoint-before-remove".
- **Dead-Feature-GC = mehr Streich-Achsen als tsc/src-grep sehen — i18n + DB-Objekte je eigene Achse** (S457/S458): Beim Entfernen eines toten Features reicht „Service/Hook/RPC/Tabelle gedroppt + tsc grün" NICHT — zwei Achsen sind für tsc/`src/`-grep **unsichtbar** und bleiben sonst als Schnitt-Regel-Verstoß zurück: (1) **i18n-Keys** (`messages/*.json`) — ein Feature mit UI-Vergangenheit hinterlässt Label-/Notification-Strings, die in keinem `src/`-Consumer auftauchen → Pflicht-Streich-Achse `grep <feature-camelCase> messages/*.json`. **Bei Notification-i18n-Keys VOR dem Löschen Live-DB prüfen** (`SELECT FROM notifications WHERE i18n_key IN (...) OR type ~* '<feature>'`) — wird der Key dynamisch über `notif.i18n_key` aufgelöst, würde Löschen einen i18n-Leak bei Bestands-Notifications erzeugen; 0 Rows = sicher. (2) **DB-Objekte** — Caller-Completeness per `pg_proc.prosrc`-Writer/Caller-Enum **UND `cron.job`** (pg_cron ruft via `command`-String, nicht über prosrc — ein reiner pg_proc-Grep verpasst Scheduler) **UND ACL** (an `authenticated`/`anon` granted = latentes A-02-Surface, GC schließt es mit). Geld-nahe Fn (mutiert money/scout_scores) braucht den 4-Wege-Check (pg_proc+pg_cron+src+ACL) BEVOR DROP, auch wenn statisch „0 Caller". DROP-Sicherheit beweisen via **force-rollback-Smoke** (`DO $$ … DROP …; RAISE EXCEPTION 'report' $$` = Tx-Rollback + Ergebnis im Fehlertext) + **Survivor-Gegenprobe** gleichnamiger lebender Objekte (distinkte Namen wie `scout_mission_definitions` vs lebend `mission_definitions`; geteilte Query-Keys wie `qk.missions.scout` BEHALTEN, nur exklusive entfernen). Live: 457 (Scoring-Modell) + 458 (season_reset_scores + Scout-Mission-System; Reviewer-Catch i18n).
- **Data Contract Changes (NICHT als UI-Change behandeln)**: required→optional (Field/Prop/Column) = Contract-Change → ALLE Consumer greppen. optional→required = Breaking → Migration+Backfill. Service swallow→throw = Breaking für Caller → alle Caller + try/catch auditen. Jede DB-Write-Änderung → `/impact` ODER manueller Grep VOR Code.
- **Shell / Hooks (Windows Git Bash)**: `grep -oP` mit `\K` scheitert silent (Locale) → `sed -n 's/.*"key"\s*:\s*"\([^"]*\)".*/\1/p'`. Worktree-Agents haben KEINEN `.claude/skills/`-Zugriff → Fallback Main-Repo-Path. `tr '[:upper:]'` ist NICHT UTF-8-aware → `LC_ALL=C.UTF-8`-Prefix ODER Dual-Pattern (lowercase + UPPERCASE) bei Umlaut-Keywords.
- **Shell case-statement wildcard promiskuoes** (S145+146): `case "$CMD" in *"merge"*)` matched jede Message mit „merge". Command-token-anchorn: Start `"git merge"|"git merge "*)` (nicht `*"git merge "*`); Flags erst Quotes strippen via `sed`, dann `case`.
- **Heredoc-Backdoor in Commit-Gates** (S145+146): `case "$COMMAND" in *"<<"*) exit 0` lässt jeden heredoc-Commit den Gate umgehen. Heredoc-Exempt weg; MSG-Extraktion via `grep -oE "(feat|fix|refactor)[(:]..."` auf COMMAND-String.
- **grep `\b` Word-Boundary broken bei JSON-escaped Heredoc** (S146): PreToolUse-Hooks bekommen `\n` als Literal `\`+`n` → Char vor `feat` = word-char → `\b` matched nicht. Fix: `\b` raus, `[(:]`-Suffix als Anker (oder PCRE `[^a-zA-Z_]`-Lookbehind). Bei JSON-Stdin-Hooks ist `\b` unreliable.
- **Hook-Merge-Pattern** (S186): Auto-generated Files (z.B. `memory/session-handoff.md`) NIE komplett überschreiben → Marker-Block-Merge (`<!-- auto:handoff-start/end -->`, awk state-machine). Bestand ohne Marker: Auto-Block oben + `---`-Separator.
- **Spec-Drift-im-Drift-Heal-Anti-Pattern** (S234): L-Slices mit Meta-Process-Scope: Counts/Tabellen in der Spec veralten während Implementation iteriert (Author-Confirmation-Bias). Mitigation: post-BUILD vor REVIEW Spec-Self-Audit-Pass (Counts+Tabellen+Stage-Chain gegen Code). Reviewer ist letzte Falle, nicht erste.
- **Issue-Closing != Bug-Resolved** (S234): Issue-State ≠ Bug-Resolution-State. Recurring Failure-Klassen brauchen einen Master-Tracker-Issue (Label `tracker`/`recurring`) der NIE geschlossen wird bis Klasse selbst gefixt; Auto-Issue-Pipelines dedup'n via title-prefix gegen ihn.
- **Master-Tracker-Pre-Check Code-Pattern** (SO-4): GHA-Auto-Issue darf nicht blind `issues.create` — erst `listForRepo({labels:'smoke-fail,beta-blocker', state:'open'})` (Komma=AND), Master via Title-Heuristik ODER ältestes-offenes → Comment statt new-Issue. Neuer Master kriegt `master-tracker`-Label. Audit: `issues.create` ohne vorheriges `listForRepo` = Risk.
- **Cold-Start-Warm-Up vor Smoke-Suite** (SO-4): Vercel-Lambda ~10-25s Warm-Boot → erster `locator.click` trifft 30s-Timeout = false-positive. curl-retry-loop (6×, 200-check + 5s settle) als Bash-Step ZWISCHEN `playwright install` und `playwright test` — kein Hard-Fail, inkrementiert keinen Test-Counter.
- **settings.json-Edit > 3 Hooks → IMPACT-Stage-Pflicht** (S234): ≥3 Hook-Registrierungen ODER 1 Hook-Removal/Replacement → IMPACT-Stage Pflicht (Hooks sind cross-cutting: Order-Mismatch, Trigger-Overlap, Latency-Regression). IMPACT-Sektion listet alle Hooks am selben Trigger-Event + verifiziert keine Order-Dependency.
- **Hook-Konsolidierung: exit-Code + Trigger-Source + Gate-Dedup** (S436): Claude-PreToolUse-Hooks blocken NUR mit `exit 2` — `exit 1` ist non-blocking → ein „Gate" das nie blockt (silent, jahrelang unbemerkt). Tool-Command via **stdin-JSON** parsen (`sed`/`jq` auf `.tool_input`); `$CLAUDE_TOOL_INPUT`-Env ist NICHT gesetzt → wer darauf prüft läuft unconditional auf JEDEM Bash. Zwei Commit-Gates (`.husky/pre-commit` git-level + Claude-PreToolUse) mit überlappenden Checks driften → in den **autoritativen** falten (husky läuft IMMER, nicht nur wenn Claude committet; Claude-Hook nur als schnelles Vorab-Feedback sinnvoll). `git diff HEAD~$N HEAD` als „die letzten N fix()-Commits" ist falsch bei interleaved Commits → `git log --since=<window> --name-only` (Zeitfenster, count-unabhängig).
- **Legacy-Tooling retiren: erst klassifizieren, dann löschen** (E0-W3/W3b): Jedes Legacy-Artefakt in 3 Klassen — Write-only/tot (Write-Block weg, archivieren) · Funktionaler Read-Dep (Consumer zuerst umstellen, DANN archivieren) · Aktives Wissen (`INDEX.md consult_when`-geroutet → NICHT anfassen). Side-Effekte am Code verifizieren, nicht am Kommentar. Hook aus `settings.json` + Script-Datei zusammen entfernen (kein wiring-Orphan). Live-verdrahtete Reste = eigener Slice.
- **Neue `audit:*:check`-Scripts nur in `.husky/` → KNOWN_ORPHANS-Pflicht** (E0-W2gov): `wiring-check.ts` scannt nur `.github/`+`.claude/hooks/` (NICHT `.husky/`) → `.husky/`-only npm-Script wird als real-drift orphan gemeldet → `audit:wiring:check` (Pre-Commit Step 4) exit 1 → JEDER Commit blockiert. Fix: `KNOWN_ORPHANS`-Eintrag in `wiring-check.ts`. Nach jedem `.husky/`-only Script SOFORT FRISCH `pnpm audit:wiring:check` (stalen Report nicht vertrauen).
- **Config-Validation-Throw auf Money/Fire-and-forget-Pfad → 500** (S369): Ein Lazy-Init-Validator (`webpush.setVapidDetails`, SDK `init()`, Key-Parsing) der bei Fehlformat WIRFT, aufgerufen aus einem Fire-and-forget-Side-Effect auf einem Money-Pfad (Push bei Order-Fill), MUSS `try/catch → degrade-to-skip + capture-once-Flag` haben. Sonst bubbled der Throw zur HTTP-Route → 500 (Trade läuft trotzdem durch → latent). Live-Beispiel: `ensureVapid()` ohne try/catch + Prod-VAPID-Secret mit Quotes+Newline (Paste-Drift bei Rotation) → `setVapidDetails` warf bei jedem Push. Heal: try/catch + `_vapidFailed`-Flag (resettet per Cold-Start/Redeploy → Recovery nach Secret-Fix automatisch) + defensiver `sanitizeVapidKey` (Wrap-Quotes/Whitespace strippen) an JEDER Secret-Env-Lesung. Manuell-gepastete Secrets driften wiederkehrend mit Quotes/Newline → Sanitizer am Read ist die Mitigation.
- **Inner-Route-`catch` der 500 RETURNT statt zu werfen = Sentry-blind** (S369): Eine API-Route mit eigenem `try/catch`, dessen catch `NextResponse.json(...,{status:500})` ZURÜCKGIBT (nicht `throw`), umgeht `withLogger`/Middleware-Observability — `withLogger.captureError` feuert NUR beim Throw (apiLogger.ts). Folge: Live-500 für Sentry unsichtbar (0 Issues trotz wiederkehrendem Fehler), nur `console.error` in Vercel-Function-Logs (rotieren). Distinkte Silent-Fail-Klasse zu `.catch(()=>fallback)`. Fix: im Route-catch explizit `captureError(err,{route,feature})` (oder re-throw). Audit: `grep -rn "status: 500" src/app/api/ | grep -B3 catch`.

- **Datums-Hook UTC vs Lokalzeit — Mitternacht-False-Positive** (S446): Ein pre-commit-Gate das „heute" via `new Date().toISOString().slice(0,10)` (=UTC) berechnet und gegen ein lokal gesetztes Datum (`updated:`, git-commit-date) vergleicht, blockt jeden Edit im Fenster lokal 00:00 bis +TZ-Offset (z.B. +0200 → 00:00–02:00, UTC noch „gestern"). `audit-knowledge.ts` Check 8 (`updated !== today`) traf das live (Slice 445). Fix: lokale Berechnung `${y}-${M}-${D}` via `getFullYear/getMonth/getDate`. Report-Dateinamen (nur kosmetisch) dürfen UTC bleiben — nur Datums-VERGLEICHE fixen. Audit: `grep -rn 'toISOString().slice(0, *10)' scripts/`.

## Beta-Launch-Ops

- **CSP blocks Sentry EU ingest** (silent error-tracking failure): Sentry EU = `https://<org>.ingest.de.sentry.io/`. `vercel.json` `connect-src` MUSS explizit `https://*.sentry.io https://*.ingest.sentry.io https://*.ingest.de.sentry.io` enthalten, sonst Events silent gedroppt.
- **Vercel "Sensitive" Flag auf NEXT_PUBLIC_* = Build-Injection-Bug**: „Sensitive" = nicht zur Build-Zeit injected → `process.env.NEXT_PUBLIC_X = undefined` im Browser (Sentry/PostHog `dsn === undefined`). Fix in Vercel-UI: Delete + Create New (NICHT Edit — Edit-Save zerstört die Var mit Placeholder).
- **Supabase Legacy JWT vs New API Keys** (Migration 2024+): Legacy JWT-Reset invalidiert ALLE Sessions (Platform-Logout — NIE live). New (`sb_publishable_`/`sb_secret_`) asymmetrisch → Rotation invalidiert keine Sessions. Zero-Downtime: New secret parallel → 4 Stellen updaten (Vercel Prod + GH Secret + `.env.local` + `.env.vercel-prod`) → Redeploy → Smoke → alten revoken.
- **Playwright Cookie Subdomain-Mismatch**: `domain:'bescout.net'` wird nicht an `www.bescout.net` gesendet → Leading dot `.bescout.net`. Cookie-Timing: i18n-Cookie VOR Login rendert lokalisierte Button-Namen → erst in Default-Locale einloggen, DANN Cookie setzen.
- **Vercel `deployment_status.target_url` in GHA = Preview-URL mit Auth-Wall**: Event liefert `<unique-deploy>.vercel.app` (Deployment Protection) statt Custom-Domain → Playwright in Auth-Wall = Timeout. Fix: GHA `env: PLAYWRIGHT_BASE_URL: https://bescout.net`.
- **GitHub Actions: Default GITHUB_TOKEN hat KEINE issues: write**: `github.rest.issues.create()` failt mit `Resource not accessible by integration`. Fix: `permissions: { contents: read, issues: write, actions: read }` am Workflow-Top.
- **Playwright Test-Timeout-Akkumulation gegen Prod**: `test.setTimeout(180_000)` reicht nicht für 10-step Suites (jeder `waitForApp()` akkumuliert). Lightweight-helper (`waitUntil:'domcontentloaded'` + main-locator visible) + global `test.setTimeout(300_000)`. Ziel: 10-Step <15s warm, <30s cold.
- **Route existence vs name assumption (Smoke-Test)**: „Mein Kader" ist Tab auf `/market` (nicht `/kader`); „Spieltag" → `/fantasy` (nicht `/fantasy/spieltag`). Audit vor Test-Writing: `ls src/app/(app)` + `grep -rn "href=\"/" src/components/navigation/`.
- **Two-lockfile drift (pnpm + npm parallel)**: `pnpm-lock.yaml` + `package-lock.json` parallel → CI 22× konsekutiv rot. Fix: `rm package-lock.json` + `packageManager:"pnpm@X.Y.Z"` + CI auf `pnpm/action-setup@v4` + `--frozen-lockfile`. Prevention: Branch-Protection `required_status_checks:[lint,build,test]`.
- **Cron-Guard API-Response-Count vs DB-Count** (S140): Externe API liefert weniger Rows als DB → Cron nutzt API-Count als Completion → bricht zu früh ab, DB-Rows unerreichbar. Fix: `dbTruthAllDone = totalDbRows>0 && (alreadyDone+newlyDone) >= totalDbRows; allDone = apiAllDone && dbTruthAllDone`. API-Count IST KEIN Proxy für DB-Completion.
- **github-script Template-Literal: verschachtelte Backticks → "SyntaxError: Unexpected number"** (S350): Literaler Backtick in einem `script:`-Template-String (z.B. Cron in Prosa `` (cron `0 4 * * *`) ``) beendet das Template vorzeitig → Rest als nacktes JS → Step crasht hart (bei `if: failure()` → Job-Fail-Email + Dedup greift nie). Fix: innere Backticks raus (`(cron 0 4 * * *)`). Detection: `grep -nE "cron \`|\` *[0-9*]" .github/workflows/*.yml` — YAML-Lint fängt's NICHT.
- **Pre-Push-Hook-Laufzeit bricht Push-Transport** (S350): Langer `.husky/pre-push` (volle vitest ~6-7 min) löst reproduzierbar `failed to push some refs` OHNE `remote:`-Meldung aus (Transport-Bruch, keine Server-Ablehnung). Detection: `git status` weiter `ahead N`, `git push --no-verify` landet sofort. Fix: Pre-Push auf schnelle Prüfung (~Sekunden) beschränken, volle Tests = CI (D94); NICHT `--no-verify` als Dauer-Workaround.
- **audit:silent-fail-Baseline driftet → CI bei JEDEM Push rot** (S350): `audit:silent-fail:check` exit 1 bei HIGH/MEDIUM-Increase über `.audit-baseline.json`; organisches Code-Wachstum (neue `.in()`-Calls, line-shift MEDIUM→HIGH) failt CI bei jedem Push (auch docs-only). Entscheidung fix vs. re-baseline: Report-Diff gegen älteren `worklog/audits/silent-fail-*.md` — neue echte Money-Path-Fails → fixen; line-shifted bestehende → `.audit-baseline.json` auf Ist-Zahlen setzen + lokal grün verifizieren.
