---
paths:
  - "src/components/**"
  - "src/app/**"
  - "src/features/**"
  - "src/lib/hooks/**"
---

# Errors: Frontend — Navigator

Stand: 2026-06-23 · **Slice 352:** Dieser Navigator (always-loaded bei `.tsx`/Hook-Edit) trägt je Pattern die **ACTIONABLE Regel inline**. Der volle Detail (Root-Cause, Code-Blöcke, Audit-greps, Reference) liegt **on-demand** in `errors-frontend-detail.md`. Siehe auch `ui-components.md`, `testing.md`.

> **Nutzung:** Jede Zeile ist die Guardrail — danach handeln. Trifft ein Pattern wirklich zu (oder brauchst du Code/Audit-grep) → `.claude/rules/errors-frontend-detail.md` öffnen, Abschnitt « *Heading* » lesen. Pattern-Namen unten = exakte Detail-Headings.

## React / TypeScript

### Checklist (immer)
- Hooks VOR early returns.
- `Array.from(new Set())` / `Array.from(map.keys())` statt Spread (strict TS).
- Modal: IMMER `open={true/false}` prop.
- PlayerPhoto: `first` / `last` / `pos` (nicht firstName/lastName).
- Barrel-Exports bereinigen wenn Files geloescht werden.
- NIEMALS `.catch(() => {})` — mindestens `console.error`.
- Cancellation Token in useEffect: `let cancelled = false; return () => { cancelled = true; }`.
- Null-Guards: `floor_price ?? 0`, `entry.rank ?? 999`.

### State / Hook / Render — Patterns (Regel inline · Detail-Heading in Klammern)

- **Defensive null-strict-equality bei optional-resolved Hook-Daten** (S265): Bei `T | null`-Hook-Returns mit Risk-/Boolean-Logik IMMER strict `=== <wert>` (z.B. `=== 0`), nie truthy `!value` — `null` ist *unbekannt*, nicht *at-risk*.
- **Selected-Item-Snapshot vs. Realtime-Update-Drift** (S273): Nie ein realtime-gepatchtes Item als `useState<Item>`-Snapshot halten → ID-as-State + `useMemo(() => list.find(id))`. Sub-Component-useEffect-deps auf stabile Felder (`fixture.id`,`.status`), nicht das ganze Object.
- **Derived-Loading aus `data === undefined`** (S283): Loading NIE aus `data === undefined` ableiten — `isLoading` UND `isError` destrukturieren, sonst hängt Error-Fall in ewigem Skeleton. Upstream-ID-Query = eigener Failure-Mode → kombinierter Error-State.
- **CLS-Quelle #1: daten-abhängiger Layout-Fork VOR Daten-Load** (S490): Ein `isX = data.length === 0`-Fork, der zwei verschieden hohe Layouts wählt (z.B. Neu-User vs Bestand auf `/`), MUSS an ein `ready`-Flag aus dem `isLoading` der speisenden Query gekoppelt sein — sonst rendert während des Ladens der „leer"-Default-Fork und swappt nach Load = großer bidirektionaler CLS (live `/` p75 0,282; Bestandsnutzer sahen kurz das ~440px Neu-User-Layout). Fix: `const ready = !query.isLoading;` + `{ready && isX && …}` / `{ready && !isX && …}` für ALLE Fork-Zweige (+ gleichartige dashboard-abhängige Blöcke wie founding-upsell `!highestPass`). **Voraussetzung dass das Gate greift:** UID/Auth beim First-Render present (S472-Server-Seed) — sonst uid=null-Frame → `enabled:false` → `isLoading=false` → `ready=true` → Default-Fork flasht trotzdem. Stationärer (geladener) Zustand bleibt byte-identisch (nur `ready &&` vorangestellt) → 0 Regression. Disabled-Query → ready=true → kein Infinite-Skeleton.
- **CLS-Quelle #2: dynamic-`loading`-Höhen-Skeleton für conditionally-null Komponente = reserve-then-collapse** (S491): `dynamic(() => import(X), { loading: () => <div className="h-NN …"/> })` reserviert NN-Höhe während des Chunk-Imports — rendert die Komponente danach `null` (eigener Query-`isLoading` ODER „keine Daten", z.B. `MissionHintList` `if (isLoading || hints.length===0) return null`), kollabiert der Skeleton → alles darunter springt hoch = CLS. Das Höhen-Skeleton hilft NUR, wenn die Komponente verlässlich Inhalt ~dieser Höhe rendert; für oft-`null`-Komponenten **verursacht** es CLS (Gegenteil von Slice-116s pauschaler Annahme). Fix: `loading: () => null` (Skeleton an den common-case `null`-Render angleichen). Audit: dieselbe Komponente an mehreren Sites verschieden geladen (`loading:`-Skeleton vs `{ssr:false}`-only) = §0-„von-allem-zwei" + CLS-Drift → alle an no-skeleton angleichen. Live: S491 (MissionHintList 3 Sites home/market/community → no-skeleton wie fantasy/manager). Offen-gleiche Klasse: SponsorBanner (return null bei kein-Sponsor).
- **Zeitbasiertes Freshness-Gate ohne aktiven Recovery-Trigger hängt für immer** (S372): Ein Gate `Date.now() - dataUpdatedAt < X` (z.B. `useIsBalanceFresh`), das einen Button/Flow blockiert, MUSS bei Stale **aktiv refetchen**, nicht nur passiv blockieren — sonst bleibt es für immer disabled, sobald der Wert älter als das Fenster wird und die einzige Refetch-Quelle (TopBar-Mount/Window-Focus) nicht feuert (Query schon woanders gemountet → Modal-Öffnen triggert trotz `staleTime:0` KEIN Refetch). Fix: `useEffect(() => { if (stale) refetch(); }, [stale, refetch])` — dep = Stale-Flag (bleibt während `isFetching` konstant true → kein Loop), `refetch` v5-stabil, In-Flight-dedup via Query-Key, fail-safe bei Fetch-Fehler (bleibt disabled statt Aktion auf falschem Wert). Symptom: „Saldo wird aktualisiert…" löst sich nie auf, Kaufen-Button stuck. Falle: gemeldete Korrelation kann Timing-Artefakt sein („Tippen vs +/−" = nur „>30s vergangen + Re-Render"), nicht die echte Root-Cause. Verwandt: S283, S268 (`placeholderData`→`dataUpdatedAt=0`).
- **PostgREST `.or()` mit User-Input** (S283): `,` `()` sind or-Parser-Syntax → User-Input strippen `q.replace(/[%_,().]/g,'')` oder quoted-literal, sonst 400-Parse-Fail → silent leere Liste.
- **Feature-Reaktivierung + Query-Ersatz: 3 Drift-Klassen** (S282): (1) Reaktivierte Daten-Quelle → Suppression-Mapping ALLER Sections neu auditieren. (2) Error-Gate NIE verschärfen — `error && data.length===0` behalten, dekorative Quellen graceful. (3) byIds-Waterfall: `combinedLoading = upstreamLoading || (ids.length>0 && byIdsLoading)`.
- **Cross-Section-Coupling-Drift bei Multi-Slot-Refactors** (S278): Neuer Slot-Type in einem Multi-Slot-Container → jede Sidebar/Mobile-Section greppen die DIESELBE Daten-Quelle zeigt → Suppression-Gate (`spotlightSlots.primary !== 'x' && .secondary !== 'x'`). Sonst Doppel-Render.
- **Dead-Wrapper-File mit transitive Lib-Lock-In** (S280): UI-Wrapper mit 0 Konsumenten aber Barrel-Re-Export bundlet 100KB+ transitive Libs in jeden Page-Chunk. Pre-Bundle-Refactor: Orphan-Wrapper-grep, dann Wrapper+Test+Barrel+`optimizePackageImports` löschen. (Erw. S305/324/326: Removal/Column-DROP deckt 4 Achsen — Code, DB, i18n, Tooling-Allowlists — grep auch `messages/ scripts/ .claude/`; vor DROP COLUMN ALLE services + SSR `supabaseAdmin`-selects + Network-Trace-Gate. **Erw. S375: 5. Achse = ungetypte Test-Fixtures** — `any`-Fixture-Objekte + extra React-Props auf gemockte Components sind tsc-unsichtbar UND vom Standard-Self-Verify `grep … | grep -v __tests__` ausgeschlossen → bei Removal Symbol-grep MUSS `__tests__` einschließen. `qk.*`-Factory-Eintrag + `USER_SCOPED_DOMAINS`-String = eigene Removal-Achsen (orphan ohne Consumer, S267). **DB-Mock-Feature-Removal:** vor Spalten-DROP `pg_proc.prosrc ILIKE`+`cron.job` prüfen wer schreibt — ein scheinbar toter Seed kann einen aktiven Mock-Cron als Writer haben → Cron unscheduln + Writer-Fn droppen VOR Column-DROP, echte Engine-Trigger getrennt erhalten.)
- **Lookup-Map indexed by ambiguous Key** (S276): Cache nie nach nicht-eindeutigem Key (`short`-Code) indizieren ohne Konflikt-Detection — sonst überschreibt letzter Insert silent (Wolfsburg↔Wolves). Non-unique Keys nur bei `list.length===1` setzen, sonst `getByKeyAndLeague(key, leagueId)`-Disambiguator.
- **Multi-Slot-State-Stores: Move- vs. Insert-Semantik** (S272): Store-Action `selectX(id, slot)` MUSS gegen `slot !== s && id !== id` filtern (beide!), und gegen ALLE Slot-Subtypes (Starter+Bench) deduplen. UI-Defense reicht nicht — DB-Server-Guard Pflicht im Money/Fantasy-Path.
- **Modal preventClose Pattern** (J2+J3): Jeder Modal mit `isPending`/`cancelling`/`selling`/`submitting` → `preventClose={isPending}` Pflicht (sonst ESC/Backdrop mitten in DB-Tx). Audit: `grep -rn '<Modal' src/components/ | grep -v preventClose`.
- **Liga/Context-Switch State-Reset via prevRef** (S254): Per-Context-Default-State → KEIN init-useEffect der `setOverride(default)` ruft (pinnt über Switch). Stattdessen `prevContextRef` reset auf null + `currentValue = manualOverride ?? contextDefault ?? fallback`.
- **Cache-Invalidation: Root-Prefix vs enumerated Keys** (S254): Domain-weiter State-Change (Liga/User/Locale-Switch) → Root-Prefix `invalidateQueries({queryKey:['events']})` (prefix-match), nicht enumerierte Sub-Keys (neuer Hook driftet sonst unbeachtet). Enumerated nur bei latenz-kritischen Einzelfeld-Updates.
- **Multi-RPC-Routing in EINEM Mutation-Hook → `onSuccess` MUSS beide Return-Shapes normalisieren** (S404): Routet ein Mutation-Hook abhängig von einem Arg auf verschiedene RPCs (`orderId ? buyFromOrder : buyFromMarket`), divergieren deren JSON-Return-Shapes oft (`buy_player_sc`→`new_balance`/`price_per_dpc` vs `buy_from_order`→`buyer_new_balance`/`price`). Ein gemeinsamer `onSuccess`, der nur EINE Shape liest (`result.new_balance`), bekommt beim anderen Pfad `undefined` → kein optimistisches Balance-Update + „?"-Preis im Success-Toast (stiller Display-Bug; tsc grün, weil beide Felder `?:` optional im Cast-Type sind). Fix: `result.new_balance ?? result.buyer_new_balance` / `result.price_per_dpc ?? result.price`. Shape gegen Live-`pg_get_functiondef` (D87) verifizieren, NICHT gegen Service-JSDoc. `??` (nicht `||`) wählen — schützt den legitimen `new_balance===0` (Wallet leergekauft). Live-Beleg: Slice 404 (Markt-Pfad) + **Slice 405 (Player-Detail-Pfad geschlossen = kanonische letzte Stelle; `usePlayerTrading.onSuccess` jetzt normalisiert, LIVE bewiesen: Header-Drop exakt + Toast kein „?")**. Single-RPC-Hooks (z.B. IPO-Buy `buyFromIpo` → `new_balance`/`price_per_dpc`) brauchen die Norm NICHT — die Regel triggert nur bei argabhängigem RPC-Switch in EINEM Hook. **Verwandt (S405 Bug B):** zeigt eine Bestätigungs-/Vorschau-Komponente den „Gesamt"/est-total eines order-gebundenen Kaufs, MUSS sie den Preis der gebundenen Order zeigen (gewählt, sonst günstigste fremde), NICHT `floor_price` — der Floor schließt eigene Orders ein (trading.md S7-303 F-1), der Kauf bucht gegen die günstigste fremde → Floor unterschätzt die Kosten gerade dann, wenn der Käufer selbst listet.
- **Client-abgeleiteter Markt-Wert (Best-Ask/Spread/Floor) wird an N Stellen gerechnet — eine Surface fixen ≠ Bug weg; Live-Render PFLICHT** (S414/S415/S416): Ein aus einer Order-Liste client-seitig abgeleiteter Wert (Best-Ask `Math.min(...sellOrders.map(price))`, Best-Bid, Spread, Depth) ist typischerweise in **mehreren** Komponenten dupliziert (Markt-Tab `OrderDepthView`, Player-Detail `OrderbookSummary` + eingebettetes `OrderbookDepth`, QuickStats bestBid, „sofort kaufbar"-Liste, SellModal Höchstes-Gesuch …) — „von-allem-N". Fixt man eine Surface (414: OrderDepthView), meldet der statische Check grün, aber die **sichtbar gerenderte** Surface (Player-Detail: OrderbookSummary) zeigt den Bug weiter (live `BESTER ASK = eigene Order`). **Regel:** bei jedem Display-Derivations-Fix (a) ALLE Komponenten greppen, die denselben Wert ableiten (`grep -rE "bestAsk|Math.min\(...orders|Math.max\(...bids|spread"`), und (b) **1× Live-Render gegen bescout.net** als DoD — statische/Unit-Checks fangen die Surface-Verwechslung NIE. Konkret: eigene Orders MÜSSEN aus jeder Kauf-Anzeige raus (`sellOrders.filter(o => !o.is_own)`), eigene Gebote aus jeder Markt-Nachfrage-Anzeige (`bids.filter(b => b.sender_id !== userId)`), weil `buy_player_sc`/`buy_from_order` nur `user_id != p_user_id` matchen bzw. `accept_offer` das eigene Gebot blockt (trading.md S7-303 F-1) — sonst zeigt die UI eine Order/ein Gebot, das der RPC beim Klick ablehnt. `PublicOrder.is_own` ist server-projiziert (kein userId-Prop nötig). **S416-Korrektur:** `OfferWithDetails` hat KEIN `is_own`, ABER erbt `DbOffer.sender_id` → Bid-Exclusion läuft client-seitig per `sender_id !== userId`, **KEIN Type/Service-Change nötig** (frühere Annahme widerlegt). **SSOT der Bid-Regel = `src/lib/orderbook.ts` (`excludeOwnBids`/`bestForeignBidCents`, cents-Rückgabe)** statt N inline-Filter. ask-Seite bleibt bewusst inline `!is_own` (boolean = kein Rechen-Drift). Live-Beleg: 415 (OrderbookSummary own-only → `return null`) + **416 (alle 4 Surfaces, e2e-Walk: eigenes Gebot → Spread „—", Fremd-Gebot → Spread 10% — selektiv korrekt)**. Generalisiert S405 (Floor schließt eigene Orders ein).
- **„Surface X liest Server" gegen den echten Render-CONSUMER verifizieren, nicht gegen das DB-Feld-Vorhandensein** (S424): Bei der Annahme „die gescorte/finale Ansicht zeigt den echten Server-Wert (also ist mein Vorschau-/Client-Change dort unkritisch)" NIE aus der Existenz eines DB-Row-Felds schließen, sondern `grep <feld>` über die Render-Schicht: zeigt 0 Konsumenten, hängt die Ansicht doch am Client-Rechner. Live-Beleg 424: `synergy_bonus_pct`/`synergy_details` existieren als DB-Type, werden aber von **keinem** Render gelesen — ALLE Synergie-Banner (Bau UND Scored) speisen sich aus dem Client-`calculateSynergyPreview`. Folge: ein „nur die Vorschau"-Fix ändert auch die gescorte Ansicht; und die finale Ansicht zeigt eine Client-Approximation statt des tatsächlich gesettelten Server-Werts (inkl. Modifikatoren wie Surge-×2). Regel: Code-Reading-Liste fragt „wer KONSUMIERT den Wert im Render", nicht „wer DEFINIERT ihn im Type". Familie von S414-416 (Surface-Verwechslung). **Update (Slice 425/426):** (a) in 425 geheilt — die scored-Ansicht liest jetzt die gesettelte `lineups.synergy_bonus_pct`/`synergy_details` (inkl. Surge), Coercion `Math.round(Number(...))` (das Feld kommt runtime als **NUMERIC-String "10.00"** via `.select('*')`-ohne-Mapping). (b) **Verschärfung:** die in 424 benannte Surface (`ScoreBreakdown.tsx`) war zudem **toter Code** — die obvious-genannte Datei kann der **verwaiste Fork-Zwilling** sein, die Live-Surface ein anderer File (`LineupPanel.tsx`). Vor jedem Surface-Fix `grep "<Component"` auf **Render-Sites** (nicht nur Import-Existenz); 0 JSX-Render = tot, woanders fixen. In 426 wurde der ganze Orphan-Fork-Cluster (6 Komponenten, komplette doppelte Builder-UI) gelöscht — `audit:orphan` + tsc-0 als Closure-Beweis (S280). Fork-Duplikate einer UI verstecken sich + verursachen Wrong-Surface-Fixes.
- **Money-Mutation invalidiert Domänen-Key aber NICHT Wallet** (S371): Eine UI-Mutation, die das Wallet belastet (Poll-Vote `castCommunityPollVote`, Research-Unlock `unlockResearch`), MUSS nach Erfolg AUCH `qk.wallet.all` (`['wallet']`) invalidieren — sonst bleibt die Header-Credit-Anzeige (`TopBar`→`useWallet`, staleTime 0) bis zum nächsten Reload stale (DB korrekt, Anzeige lügt). Trading-Pfad macht es via `invalidateWallet`/`setWalletBalance`. Regel: bei jeder neuen credit-belastenden Mutation prüfen „wird der Wallet-Key invalidiert?" (performance.md: invalidateQueries nach Writes). Gilt analog für jede balance-ändernde Community/Event/Fantasy-Aktion.
- **Filter-as-audience-choice vs Filter-as-result-filter** (S254): Top-Level-Navigations-Filter (Liga/Country/Sprache) NIE aus dem Result-Set ableiten (Catch-22: leer→Filter unsichtbar→kein Switch). Static-Source `getCountries(locale)`. Result-Filter nur als Verfeinerung innerhalb bereits-gewählter Audience.
- **Non-reaktiver Module-Cache + useMemo-stale-deps → Cold-Load-Race** (S286): Render-time `useMemo(() => getCountries(locale), [locale])` auf async Module-Cache → captured leer, recomputet nie (Cold-Load/PWA leer). Fix: Cache-Version-Signal via `useSyncExternalStore` + als zusätzliche useMemo-dep. Click-/Effect-Reads sind safe.
- **TanStack v5: `initialData` vs `placeholderData` für Cold-Start-Mirror** (S268): UID-keyed Mirror-Hooks NIE `initialData` (persistiert als fresh data → `dataUpdatedAt`-Freshness-Gating lügt = Money-Bug + Cross-User-Race). Immer `placeholderData` (`dataUpdatedAt=0`, status bleibt pending).
- **Server-Auth-Seed (SSR des authed Surface) × synchroner localStorage-cached `placeholderData`/`initialData` = Hydration-Mismatch #418/#423** (S474): Sobald ein Slice den User **server-seitig seedet** (S472: `getServerUser`→`initialUser`→AuthProvider, sodass `userId` beim First-Render präsent ist), divergiert JEDER `userId`-gated Hook, der einen UID-keyed localStorage-Mirror **synchron im Render** liest (`useMemo(() => readCached('bs_wallet'/'bs_tickets', userId))` als `placeholderData`): Server (kein window) → undefined → Skeleton; Client → cached Wert (z.B. „12.501,47") → React #418 (Text-Node-Mismatch) + #423 (voller Root-Re-Render → der ganze SSR-LCP-Win zunichte). Der `useMemo`-statt-`useState`-Schutz (S268) hält NUR solange `userId` server-seitig `undefined` ist — der Seed bricht die Annahme. **Fix-Pattern:** post-mount-gated Cache-Read (`useState(false)` + `useEffect(setHydrated(true))` → `hydrated && uid ? readCached : undefined`), kanonisch in EINEM SSOT-Hook (`src/lib/hooks/useCachedPlaceholder.ts`), nicht pro Hook dupliziert. First-Render dann server==client (beide Skeleton), Cache 1 Tick später; `dataUpdatedAt` bleibt 0 → Money-Freshness-Gate (`useIsBalanceFresh`) unberührt. **Vollständigkeit:** `readCached` typ-beschränkt (`bs_wallet`|`bs_tickets`) → nur 2 Konsumenten; allgemein ALLE synchron-localStorage-im-Render-Hooks greppen. React-Query-Persist (QueryProvider) scheidet als Quelle aus wenn die Domain in `USER_SCOPED_DOMAINS` steht (nie persistiert). **Diagnose-Lehre (teuer):** der un-minifizierte Mismatch (»Expected server HTML to contain a matching text node for "X" in <div>« + Pseudo-HTML-Pfad zur Komponente) steckt NUR im **Next.js Dev-Error-Overlay** (`nextjs-portal` shadowRoot, „1 error"-Badge klicken) — die Prod-Console zeigt nur minifiziertes #418 „(0 args)". `next dev` eingeloggt = schnellster Weg zum exakten Text-Node. **Falle:** localhost UND bescout.net haben Service-Worker + Cache (`bescout-v4`) → nach Code-Change SW deregistrieren + Caches löschen + hard-reload (`navigator.serviceWorker.getRegistrations().forEach(r=>r.unregister())` + `caches.keys().then(k=>k.forEach(c=>caches.delete(c)))`), sonst rendert der Client ALTE Chunks gegen frische SSR und der Fix scheint „nicht zu greifen". Familie von Reviewer-S472-F1 („SSR des authed Surface = Blast-Radius, nur Live-Walk clearbar") + S397/398.
- **@tanstack/react-query `HydrationBoundary`/`dehydrate` direkt im RSC-Server-Component importiert = Dual-Build-Crash (»No QueryClient set«)** (S476): Importiert ein Server-Component (`page.tsx`, kein `'use client'`) `HydrationBoundary` (oder `useQueryClient`-nutzende Exports) direkt aus `@tanstack/react-query`, resolved der Server-Build-Context zu `build/**legacy**`, während der Client-`QueryClientProvider` zu `build/**modern**` resolved → **zwei React-Context-Instanzen** → die legacy-HydrationBoundary findet den modern-Provider nicht → »No QueryClient set« → die GESAMTE Page crasht in die Error-Boundary (#422). **Fix:** HydrationBoundary in eine `'use client'`-Wrapper-Komponente auslagern (resolved modern, matcht Provider); `dehydrate(queryClient)` darf im Server-Component bleiben (braucht den prefetch-Client; `DehydratedState` ist build-agnostisches JSON, das der modern-`hydrate` korrekt liest). **Detektion:** der Bug ist NUR im un-minifizierten Component-Stack sichtbar — die Import-Pfade `…/build/legacy/HydrationBoundary` vs `…/build/modern/QueryClientProvider` sind der Smoking-Gun; Prod-Console zeigt nur minifiziertes #422/»No QueryClient set« ohne Pfad → `next dev` reproduzieren (public Page = kein Login nötig). **Prozess-Lehre:** SSR-Slices MÜSSEN jeden strukturell-distinkten Page-Typ im Live-Walk abdecken — `/club/[slug]` (einzige Page mit 471-HydrationBoundary + bypasst AuthGuard) war seit Slice 471 komplett kaputt, undetektiert, weil home/market/fantasy/player den Typ nicht abdecken. Live-Beleg: Slice 476 (Heal des 471-Bugs).
- **SSR-LCP-Slice: ALLE Terme des Render-Gates gegen den SSR-Zustand prüfen, nicht nur den Daten-Lade-Term** (S493): Soll ein bisher client-only Subtree (Hero/LCP-Element) ins SSR-HTML kommen, indem man das `if (authLoading || dataLoading) return <Skeleton/>`-Gate im SSR passieren lässt, reicht es NICHT, nur `dataLoading` per Prefetch false zu machen — **jeder** Boolean-Term muss beim SSR false sein. Falle seit Slice 472: `AuthProvider.loading = (initialUser == null)` → für **ausgeloggte** Besucher ist `authLoading` beim SSR **true** (bewusstes 472-Design „logged out → SSR skeleton") → der Hero bleibt Skeleton trotz Daten-Prefetch. **Cheap-Proof VOR Deploy: `curl <url>` (anon, ohne Cookies) → enthält das SSR-HTML den LCP-Knoten (`<img>`/h1) oder Skeleton-Markup (`animate-pulse`-Count)?** — nicht erst ein Live-LCP-Trace. **Fix-Muster (Auth-Gate-Decouple):** `getServerAuthState`-Helper (`cache()`-wrapped → dedupt mit dem layout-Auth-Seed = 1 getUser()/Request) der `{user, resolved}` zurückgibt; `resolved=false` bei getUser-Fehler. page.tsx → `ssrConfirmedAnon = resolved && user===null` (serialisierter boolean-Prop) → Gate `(authLoading && !ssrConfirmedAnon) || loading`. Der `resolved`-Discriminator ist Pflicht: nur `user===null` würde bei transientem getUser-Fehler eines EINGELOGGTEN Users die anon-Ansicht im SSR rendern → Client authed → #418-Content-Swap; mit `resolved` fällt der Fehlerfall fail-safe auf Skeleton. Server+Client teilen dieselben Cookies + denselben cache()'d Helper → bestätigt-anon stimmt garantiert überein. Live: Slice 493 (/club anon LCP 2226→1269ms/−43%, render-delay 1486→545, Hydration sauber, authed 0 Regress; R1-Reviewer fing den übersehenen authLoading-Term). Verwandt S474/476.
- **LCP-Mess-Confound: Auth-Zustand des Mess-Browsers VOR jeder Messung verifizieren** (S493): Ein persistenter Login im chrome-devtools/Playwright-Browser (Session aus früherer Session) verfälscht „ausgeloggt"-LCP-Traces still — bei einem auth-abhängigen Render-Pfad (z.B. /club: authed skeletont weiter wg. club-key-mismatch, anon nicht) misst man den falschen Pfad und der Fix scheint „flach". Live 493: die ersten 3 anon-Traces (3098/2343/1992ms) waren der eingeloggte /club (Ali-Session) → schienen den −43%-Win zu widerlegen; erst nach Logout (Abmelden braucht Bestätigungs-Klick) sauberer anon-Trace = 1269ms. Regel: vor jeder auth-sensitiven LCP-Messung 1× Snapshot auf ein Auth-Diskriminator-Element (Follow-Button=authed vs Register=anon; Sidebar-Wallet=authed). Mehrfach messen (LCP-Varianz hoch) + TTFB-Anteil separat lesen (Cold-Start vs persistent).
- **Map/Set-typed React-Query-Data + Persist/SSR = stille Korruption** (S267): Service-Return `Promise<Map/Set>` überlebt `JSON.stringify` nicht (→`{}`) → `.values is not a function`-Crash. 3-Layer: Persist-Filter (`data instanceof Map → false`) + defensive Map-Reconstruction im Hook + buster-bump. Besser langfristig: `Array<[K,V]>` returnen.
- **Multi-League Props-Propagation** (J3+J4): Jedes Type mit `club*`-Field MUSS spiegelbildlich `league*`-Field haben; neues optional Field → ALLE Render-Call-Sites manuell greppen (optional = kein TSC-Error, Pilot-QA übersieht's).
- **Data-Format vs Component-Expectation Drift** (S102): Component-API-Contract im Service-Mapper erzwingen — leer/unbekannt → `""`/`undefined`, NIE raten (Pilot-Default `?? 'TR'` wird bei Multi-Liga zum Gift). Truthy-Check `{code && <Flag/>}`.
- **Display-Anker aus Source-of-Truth, nicht aus vergifteter denormalisierter Spalte** (S368b): Ist eine denormalisierte/Convenience-Spalte bekannt mock-/legacy-/migrations-überschrieben (z.B. `players.ipo_price` durch Slice 114 auf MV/10 für JEDEN Spieler → erfundener „Einstieg" auch ohne IPO; analog `dpc_mastery.hold_days`-Seed S367), liest die UI den **autoritativen Quell-Datensatz** (hier `ipos.price` der Erst-IPO via eigenem lazy Read-Hook). Fallback = **ehrliches „—"**, NICHT erfundene Zahl, und NIE Backfill der vergifteten Spalte (Slice-114-Klasse verboten, vgl. errors-db.md S303). Mock→Pro-Default: bei jedem Anzeige-Wert fragen „kommt der aus der Wahrheit oder aus einer bequemen, evtl. geseedeten Spalte?".
- **ConfirmDialog statt native alert/confirm** (J4): `src/components/ui/ConfirmDialog.tsx` (preventClose + loading + `confirmVariant`). Native alert/confirm verboten (unstyled, blockt Main-Thread, nicht i18n). Audit: `grep -rn 'window.alert\|window.confirm' src/`.
- **Vote-Toggle Client-Intent vs RPC-Constraint** (S159→160): UI sendet IMMER `1`/`-1` (nie `0` für Toggle-Off; RPC blockt `NOT IN(1,-1)` mit `{success:false}` → silent undefined). Handler ermittelt `isToggleOff = prevVote === voteType`.
- **React setState Race in Mutation-Handler** (S149-151,D18): `if(loading)return;setLoading(true)` ist race-prone (2 Clicks/Frame = 2 DB-Writes). `useSafeMutation` (synchroner `isPending`-Guard), Money-Path `useSafeIdempotentMutation`. Audit: `npm run audit:mutation-race`.
- **Component-Prop Silent-Fallback** (S149b,D17): Fallback-Branch mit schlechter UX + `prop?: T|null` → Call-Sites lassen prop weg OHNE TSC-Error (30% silent-fail). Prevention: prop **required** + Caller `prop={x ?? null}` explicit.
- **Singleton→`useQueryClient()` Migration exhaustive-deps-Trap** (S170→171): Hook-lokales `useQueryClient()`-Binding MUSS in alle deps-arrays die `queryClient.*` lesen (Module-Import war exempt). Post-Migration: jede enclosing `useCallback` deps prüfen.
- **Realtime-Hook-Refactor: TanStack-Query → Subscription-only callback** (S267): Konsument nutzt `useState<T[]>` → Realtime-Hook MUSS Subscription-only callback-Pattern sein (`onUpdate`-ref, deps nur `[scopeId]`), NICHT `setQueryData`. Sonst Doppel-Fetch + State-Drift. Code-Reading-Liste MUSS Konsument-File enthalten.
- **qk-Key-Definition ohne Konsument** (S267): Neuer `qk.*`-Key ohne Verwender = orphan-Export. Detection-grep, dann Delete ODER JSDoc-Reserve-Comment.

### CSS / Tailwind Gotchas
- `::after`/`::before` mit `position:absolute` → Eltern MUSS `relative` (`overflow:hidden` reicht NICHT als Containing Block).
- `flex-1` auf Tabs → iPhone-overflow → `flex-shrink-0`.
- Dynamic Tailwind NIEMALS `border-[${var}]/40` (JIT scannt nur statische Strings) → `style={{ borderColor: hex }}` + statische Class.
- **`gold-pulse-bg` ist statischer Gradient** (S261): Pulse braucht zusätzlich `motion-safe:animate-pulse motion-reduce:animate-none` — der Klassenname allein animiert nicht. Detail: errors-frontend-detail.md.
- **Tailwind `data-*` Variants nur auf Tailwind-Utilities** (S181): `data-[state=open]:anim-modal` wirkt NUR wenn `anim-modal` in `@layer utilities` steht (nicht plain global-CSS) — sonst keine Animation. Detail: errors-frontend-detail.md.

### UX Konsistenz
- Spieler-Anzeigen → Link zu `/player/[id]` (Ausnahme: Picker-UIs).
- `<button>` NICHT in `<Link>` wrappen (invalid HTML) → `href`-Prop oder Wrapper.

## i18n / Locale

- **Dormanten Pfad sichtbar machen → Live-Render-Audit der neu-gerenderten geteilten Downstream-Komponenten** (S397/398): Ein bisher unerreichbares Feature zu verkabeln (z.B. User-Events sichtbar machen, ein neuer Event-/Card-/Modal-Typ) rendert erstmals geteilte Downstream-Komponenten (EventCard, EventDetailModal, **BenchRow**), in denen **latente Gaps schlummern** — fehlende i18n-Keys (`fantasy.bench*` waren seit Feat 195d NIE definiert → 95 MISSING_MESSAGE + **Roh-Key-Leak in der sichtbaren UI**) und währungsunabhängige Display-Zeilen (`{ticket_cost} Tickets` statt Credits bei `currency='scout'`). tsc + Unit fangen das NIE (i18n/Display = runtime). Regel: nach dem Verkabeln eines dormanten Pfads **1× Live-Render gegen bescout.net** mit Console-Scan auf `MISSING_MESSAGE` + Augen-Check der geteilten Card/Modal-Labels — die first-visible-render-Stelle ist wo die Gaps feuern. Frontend-Spiegel von errors-db S396 („latenter CHECK feuert erst beim ersten echten Pfad"). Verwandt S198 (missing-key)/S333 (falscher Namespace).
- **JSON Object/String-Duplicate-Key-Drift** (S263): Neues Sub-Object darf keinen Top-Level-String gleichen Namens im selben Namespace haben (JSON last-wins, String unreachable, latent React-Crash). Bei jedem neuen Sub-Object prüfen + Top-Level-String löschen wenn 0 Consumer.
- **i18n-Key-Leak via Service-Errors** (J1+J3): Service wirft I18N-KEYS (`throw new Error('handleReserved')`), Consumer resolved via `te(mapErrorToKey(normalizeError(err)))` — NIE `setError(err.message)` (zeigt raw key). Nach jedem swallow→throw alle Consumer-Pfade greppen.
- **Missing i18n-Key bei neuer CTA-Component** (S198): Nach JEDEM neuen `t('namespace.key')` in einer Component SOFORT DE+TR bedienen (`defaultMessage`-Fallback = TR sieht DE-String, kein TSC-Error). **Erw. S333:** Key kann im FALSCHEN Namespace-Objekt liegen (grep grün, Runtime `MISSING_MESSAGE`) → namespace-aware Node-Check + 1× Live-Render-Console-Scan gegen bescout.net Pflicht.
- **Gerades ASCII-Quote `"` in einem `messages/*.json`-String-Value bricht JSON** (S399): Eine deutsche Quote-Klammer um einen ICU-Platzhalter (`"„{name}" wirklich absagen?"`) mit GERADEM schließendem `"` (U+0022) beendet den JSON-String vorzeitig → `JSON.parse`-SyntaxError, der ganze next-intl-Load crasht (nicht nur der eine Key). Tückisch: das **Edit-Tool normalisiert** ein getipptes typografisches `"` (U+201C) oft auf das gerade `"` zurück (old==new), sodass der Fix scheinbar nicht greift. Robuster Fix: per `node`/`perl` mit `\uXXXX`-Escapes operieren (encoding-sicher), ODER die inneren Quotes ganz weglassen (`"{name} wirklich absagen?"`). Pflicht-Gate nach JEDER `messages/*.json`-Änderung: `node -e "JSON.parse(require('fs').readFileSync('messages/de.json','utf8'))"` für de+tr (fängt es deterministisch; tsc/grep NIE). Nur typografische `„…"` (U+201E…U+201C) sind im JSON-Value safe — gerade `"` immer escapen oder vermeiden.
- **RPC-Reject-Code ohne `mapErrorToKey`-Mapping → stiller `'generic'`-Toast (UX-Gap, kein Leak)** (S393): Gibt eine RPC discriminierte Reject-Codes zurück (`rpc_save_lineup` → `age_max_exceeded`, `nation_not_allowed`, …) und der Service wirft sie als `err.message`, MUSS jeder Code in `errorMessages.ts` `KNOWN_KEYS` (snake_case-Passthrough, analog `bench_*`) **oder** in `ERROR_MAP` (Regex) stehen UND eine DE/TR-Zeile im `errors`-Namespace haben — sonst fällt `mapErrorToKey` auf `'generic'` (Z.181) und der Nutzer sieht „Ein Fehler ist aufgetreten" statt der konkreten Regel. Kein Crash, kein MISSING_MESSAGE (`'generic'` existiert), aber granulare Regeln/Guards werden für den Nutzer un-unterscheidbar → der Sinn der Granularität verpufft. Bei JEDER neuen reject-code-erzeugenden RPC: Codes (`pg_get_functiondef`-`'error'`-Literale) gegen `KNOWN_KEYS ∪ ERROR_MAP` + `errors`-NS (DE+TR) diffen. Verwandt S330 (4-File-Sync), S379 (Enum-Drift mehrere Gate-Flächen). Reicher Kontext (`limit`/`age` im Validator-Return) geht verloren, wenn der Service nur `result.error` wirft — Threading = eigener Throw-Refactor. **Erw. S455:** ein neuer Reject-Code mit gemeinsamem Präfix hat ZWEI FE-Surfaces mit verschiedener Match-Semantik — der `switch(msg)` in `useEventActions.ts` ist **Exact-Match** (`case 'x'`, verfehlt Suffix-Varianten wie `insufficient_sc_bench` → fällt in `default` → generisch-gewrappter `errorGeneric`-Toast; KEIN Raw-Leak, weil die mapErrorToKey-Regex `/insufficient_sc/i` im default-Pfad greift, aber die *spezifische* Toast mit `{min}` verpufft), während `mapErrorToKey` **Regex** ist (fängt Suffix). Bei jedem neuen Reject-Code mit Präfix-Verwandtschaft BEIDE Surfaces prüfen: den `switch`-`case` ergänzen (ggf. kombiniertes `case` mit dem Präfix-Code = gleiche Toast), nicht nur auf mapErrorToKey verlassen.
- **Polish-Audit Pre-Existing-Code-Drift** (S200a): Vor „X fehlt"-Klassifikation den consumed Hook/Service greppen (`src/features/<d>/hooks/ services/ src/lib/hooks/`) — Logik existiert oft schon → neuer useEffect = Duplicate. Reviewer-Agent als Backup-Catcher.
- **Service-Duplicate bei parallelem BE+FE-Dispatch** (S199,D46): BE+FE-Worktrees bauen denselben Service doppelt → einer wird orphan (TSC clean). SPEC-Sektion „Service-Schnittstelle" (kanonischer Pfad+Signatur) Pflicht bei Cross-Domain. Post-merge: duplicate-export-grep.
- **Hardcoded German addToast/Error-Strings** (S196): `addToast('Ein Fehler...', 'error')` umgeht i18n → TR bricht. `addToast(te(mapErrorToKey(...)), 'error')`. Audit: `grep -rn "addToast\('[A-Z]" src/ | grep -v "bescout-admin\|__tests__"` (admin+tests exempt).
- **Error-Messages nie dynamische Werte** (J3): `throw new Error(\`Price exceeds ${X} $SCOUT\`)` = DE/EN-Mix + $SCOUT user-facing + dynamischer Wert. Dynamic gehört in Pre-Submit-Hints, nicht Post-Error.
- **Turkish Unicode**: `'I'.toLowerCase()` = `i̇` (nicht `i`) → NFD + strip diacritics + `ı→i`. SQL: `translate(lower(name),'şçğıöüİŞÇĞÖÜ','scgiouISCGOU')`.
- **Money-RPC Idempotency-Window** (151c.2): Historisch (inline-60s), abgelöst durch generic S178a-f → siehe `errors-db.md` Money-RPC Idempotency-Blueprint.
- **useCountUp auf volatile Server-Daten** (S151b,D20): `useCountUp` animiert jedes Re-Render → auf React-Query-Daten 2-3 Desync-Zyklen. `useDeferredValue` davorschalten. Audit: `grep -rn 'useCountUp(' src/ | grep -v useDeferredValue`.
- **PLAYER_SELECT_COLS Sync mit DbPlayer-Type** (S200): Bei JEDEM `ALTER TABLE players ADD COLUMN`: DbPlayer-Type + dbToPlayer-Mapper + **`PLAYER_SELECT_COLS`** (oft vergessen → Spalte kommt nie zurück, immer `null`, TS-Cast lügt) + Test gegen echte DB. Gilt analog für andere `*_SELECT_COLS`.
