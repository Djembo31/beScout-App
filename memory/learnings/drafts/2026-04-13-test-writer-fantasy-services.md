**2026-04-13 -- Test Writing (Fantasy Services)** / The shared mock at `@/test/mocks/supabase` v2 API (mockTable/mockRpc/resetMocks) is the canonical pattern for new tests. The hoisted vi.mock pattern in scoring-v2.test.ts is legacy. Fantasy services in `features/` swallow DB errors (return []/null/0) unlike `lib/services/` which increasingly throw. mapStatRow has a two-tier null fallback: FK player join -> parseApiName -> empty string. / Confidence: high
