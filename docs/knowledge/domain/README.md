# domain/ — wie funktioniert X

Mechanik & Funktionsweise von BeScout-Systemen: **wie** etwas arbeitet, nicht warum es so entschieden wurde (→ `decisions/`) und nicht wie man es codet (→ `.claude/rules/`).

**Gehört hierher:** Treasury-Geldfluss · Polls-Mechanik · Reward-/Ranking-Ökosystem · Scout-Card-Trading · Fantasy-Scoring · Equipment · Produkt-Map/Vision/Feature-Dependencies.

**Gehört NICHT hierher:** Code-Patterns (`.claude/rules/`) · Entscheidungs-Begründungen (`decisions/`) · Tages-Status (`memory/session-handoff.md`).

Front-matter Pflicht (siehe `../README.md`). Migration aus `worklog/concepts/`, `wiki/`, `memory/semantisch/produkt/` erfolgt in E0-W2b.
