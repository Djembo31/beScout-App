SLICE 497 — D-08 getSystemStats „Scout Total" uncapped — PROOF
===============================================================

[PROBLEM — Live-verifiziert]
getSystemStats (platformAdmin.ts:43): wallets.select('balance').limit(5000) client-SUM.
PostgREST cappt real ~1000 (common-errors: ".limit(5000) ist KEIN Override" = MONEY-CRITICAL).
Feeds "Scout Total" StatCard im Platform-Admin (BescoutAdminContent.tsx:63).

Live-DB (skzjfhvgccaeplydsunz, 2026-07-01):
  wallet_count           = 128
  true_sum_balance_cents = 850.935.524
  capped_1000_sum_cents  = 850.935.524   (128 < 1000 → identisch)
  undercount_cents       = 0             → LATENT heute, falsch ab >1000 Wallets

[SEMANTIK-PARITÄT (D87, live pg_get_functiondef('get_treasury_stats'))]
  'total_circulating_cents', COALESCE((SELECT SUM(balance) FROM public.wallets), 0)
= EXAKT die alte totalBsdCirculation-Semantik (SUM balance), aber server-seitig + uncapped.
get_treasury_stats: SECURITY DEFINER, gated auf platform_admins (RAISE 'Not authorized' sonst).

[FIX — src/lib/services/platformAdmin.ts getSystemStats]
- supabase.from('wallets').select('balance').limit(5000)   // Query-Index 1
+ supabase.rpc('get_treasury_stats')
- const wallets = walletsRes...; totalBsdCirculation = wallets.reduce(...balance)
+ const treasury = treasuryRes.value.data as {total_circulating_cents?};
+ totalBsdCirculation = Number(treasury?.total_circulating_cents ?? 0)
§0-Subtraktion: gecappter Client-SUM (2. Weg) entfernt → kanonische get_treasury_stats-SSOT.

[STATIC PROOF]
- npx tsc --noEmit → exit 0
- npx vitest run platformAdmin-treasury.test.ts → 9/9 passed
- Consumer: nur BescoutAdminContent (SystemStats-Shape unverändert → 0 Impact)

[LIVE PROOF — Admin-Dashboard post-Deploy]  (Ziel: no-regress + SSOT-Konsistenz)
  <pending: Login ali (platform superadmin) → /bescout-admin → "Scout Total" rendert korrekt
   (850.935.524-basiert, kein 0/Fehler) UND == Treasury-Tab "circulating" (beide total_circulating_cents).>
  Hinweis: latenter Fix → heute (128 Wallets) KEINE sichtbare Wert-Änderung; Live-Check = no-regression +
  RPC-Auth-Pfad im Deploy funktioniert + SSOT-Konsistenz Overview==Treasury-Tab. Der >1000-Divergenz-Nutzen
  ist by-construction (functiondef ohne LIMIT) bewiesen, nicht live demonstrierbar ohne 1000 Wallet-Seed.
