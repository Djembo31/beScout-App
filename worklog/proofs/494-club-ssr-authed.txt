=== Slice 494 — W6/D-03 Teil 2: authed /club Hero ins SSR-HTML (user-scoped Club-Prefetch) — PROOF ===
Datum: 2026-07-01

--- STATISCH (BUILD) ---
[AC1] tsc --noEmit → EXIT 0.
[Change] page.tsx: getServerAuthState nach oben → ssrUserId = serverUser?.id ?? null.
        Club-Prefetch Key `bySlug(slug, ssrUserId ?? undefined)` + RPC `p_user_id: ssrUserId`.
        (Vorher: hart `bySlug(slug, undefined)` + `p_user_id: null` = nur anon-Key.)

[Parität — Live-DB verifiziert]
  - get_club_by_slug: uses_p_user_id=true, uses_auth.uid()=false, is_secdef=true, args (p_slug text, p_user_id uuid)
    → supabaseAdmin.rpc(..., {p_user_id: userId}) == authed-Client (useClubBySlug(slug,userId) → getClubBySlug → same RPC/p_user_id). Byte-identisch.
  - Client-Key: useClubBySlug(slug, userId) → qk.clubs.bySlug(slug, userId) → matcht Prefetch-Key.
  - useCachedPlaceholder (S474 localStorage-#418-Falle): NUR useWallet + useUserTickets (SSR-gehärtet seit 474),
    NICHT useClubData → authed ClubContent-Baum hat keinen neuen #418-Vektor.
  - players-Prefetch unverändert (public, gleicher Key für alle User).

[AC2 anon Regress] ssrUserId=null → `bySlug(slug, undefined)` + `p_user_id: null` = byte-identisch zu 493.
        ssrConfirmedAnon-Logik unverändert. → anon-Pfad (493 −43% Win) unberührt.

--- LIVE-WALK (PROVE, post-Deploy 6c74736d, bescout.net, eingeloggt ali) → 🔴 REGRESS → REVERTED ---
[AC3 authed] chrome-devtools authed /club: **React #425 (Text-Content-Mismatch) + #422 (Hydration-Error)**
        in der Console. LCP 1824ms / render-delay 914ms (KEIN SSR-Win — LCP-Element ist zudem below-hero).
        → **494 hat die authed-Hydration GEBROCHEN.** Der volle authed ClubContent-Baum rendert server-seitig
        mit teils leeren Sekundär-Daten (Standings/Events/Trades NICHT geprefetcht → leer server, geladen client)
        → Text-Mismatch. Die anon REDUZIERTE PublicClubView (493) war safe; die authed VOLLE ClubContent ist es NICHT.
        = exakt die S472/476-Blast-Radius-Warnung, live materialisiert (nur Live-Walk clearbar, kein Agent-Review).

VERDIKT: 🔴 **494 REVERTED** (page.tsx auf 493-Stand, Commit-Revert). Gründe:
  - Echter Hydration-Regress (#425/#422) auf authed /club — inakzeptabel.
  - LCP-Nutzen ohnehin marginal (authed LCP-Element = below-hero, nicht der Hero → render-delay 914ms).
  - /club authed = non-core (Eingeloggte nutzen home/market/fantasy). Fix (alle authed-Sektionen SSR-safe
    machen = below-hero-Streaming-Refactor) = großer Scope, unverhältnismäßig. §1 caution-over-speed.
  - anon 493-Win (−43%) bleibt vollständig erhalten (page.tsx-493-Stand rendert anon-Hero weiter im SSR).

LEHRE: Reduced-view-SSR (anon PublicClubView) ≠ full-view-SSR (authed ClubContent). Ein SSR-Slice der einen
GROSSEN client-Baum mit vielen sekundär-daten-abhängigen Sektionen erstmals server-rendert braucht ENTWEDER
alle Sektionen SSR-safe (empty-state == client-first-render) ODER below-hero-Streaming (nur Hero aus SSR,
Rest client-gated). Live-Walk BEIDER Auth-Zustände ist Pflicht — der authed-Regress war statisch unsichtbar.

--- REVIEW ---
self-review + Live-Walk (der den Regress fing = der eigentliche Gate). Reverted, kein Merge-in-Prod-Schaden
über den kurzen Deploy hinaus (sofort zurückgerollt).
