SLICE 495 — anon /club resolveExpiredResearch-Gate — PROOF
============================================================

[PROBLEM — Evidence]
proof 493 Z.58 (live anon /club Console, 2026-07-01):
  "401 + permission denied for function resolve_expired_research / get_club_news_teasers"
useClubData.ts:43-45 feuerte resolveExpiredResearch() ungated bei jedem /club-Mount, auch anon.

[LIVE-DB-VERIFIKATION (Projekt skzjfhvgccaeplydsunz, 2026-07-01)]
(a) Grants — beide RPCs REVOKE anon:
  proname                    | anon_exec | auth_exec | service_exec
  get_club_news_teasers      | false     | true      | true
  resolve_expired_research   | false     | true      | true
  acl: {postgres=X/postgres,authenticated=X/postgres,service_role=X/postgres}
  → anon-Call scheitert deterministisch (permission denied). Korrekt gesichert.

(b) pg_cron nach resolve_expired_research:
  SELECT jobid,schedule,jobname,command FROM cron.job WHERE command ILIKE '%resolve%research%';
  → [] (0 Zeilen)
  → KEIN Cron. Der Client-Trigger ist der EINZIGE Auflösungspfad → nicht entfernen, nur gaten.

[FIX — src/components/club/hooks/useClubData.ts]
- useEffect(() => {
-   resolveExpiredResearch().catch(err => console.error('[Club] Resolve expired research failed:', err));
- }, []);
+ useEffect(() => {
+   if (!userId) return;
+   resolveExpiredResearch().catch(err => console.error('[Club] Resolve expired research failed:', err));
+ }, [userId]);
authed: triggert weiter (einziger Pfad intakt). anon: no-op (kann's ohnehin nicht).

[STATIC PROOF]
- npx tsc --noEmit → exit 0
- npx vitest run useClubData.test.ts → 27/27 passed

[LIVE PROOF — anon /club Console post-Deploy]  (Vercel 598046a7 Ready, Playwright anon bescout.net/club/galatasaray)
Mess-Hygiene (493-Lehre): Browser war persistent eingeloggt (sb-auth-token 2878B + Credits-UI)
→ Session/Cookies/localStorage/SW/Caches geleert → anon bestätigt (sbCookieCount=0, keine Credits-UI,
Seite gerendert) VOR der Console-Messung.

Sauberer anon /club Console (settled):
  ✅ resolve_expired_research: 0× — WEG (Slice-495-Fix greift, kein 401, kein permission-denied)
  ⏳ VERBLEIBEND (pre-existing, NICHT Scope 495):
     - get_club_news_teasers        → 401 + "permission denied" (CEO-Scope, Scope-Out oben)
     - rpc_get_club_recent_trades   → 401 (×N React-Query-Retries)
     - get_event_player_pick_rates  → 401 (×N React-Query-Retries)
  Alle drei anon_exec=false (Live-DB verifiziert) → anon-Client feuert authed-only Read-RPCs.
  = SEPARATER Befund "anon /club feuert authed-only Reads" (siehe log.md 495 Notes + Report).

Artefakt-Klärung (Ehrlichkeit): ein Mess-Zwischenlauf zeigte zusätzlich get_club_by_slug-401 +
7× "[AuthProvider] Profile load failed after retry" — das war ein Cookie-Clear-Mid-Session-Artefakt
(stale Auth-Refetch), im sauberen anon-Reload NICHT vorhanden → KEIN echtes anon-Problem.

AC-Ergebnis: (1) ✅ resolve_expired_research aus anon-Console eliminiert. (2) authed-Pfad
unverändert (feuert weiter; authed Console 0 Errors). (3) ✅ tsc 0 · useClubData.test 27/27.
