=== Slice 144c Proof (final, post-NITPICK) — 2026-04-22 ===

Change: last_squad_check wird bei transfer-detected (ohne --allow-transfers)
        trotzdem UPDATEd, damit Retired-Detection nicht faelschlich 'nie
        gescraped' sieht.

Post-Review (NITPICK #1): Log-wording nach dry-run vs prod-run gesplittet.
  - dry: '(dry: would set last_squad_check only)'
  - prod success: '(metadata skipped; last_squad_check set; use --allow-transfers...)'
  - prod error: '! UPDATE <name> (last_squad_check): <msg>'

=== tsc --noEmit ===
tsc exit: 0

=== git diff --stat ===
warning: in the working copy of 'scripts/tm-squad-scrape-local.ts', LF will be replaced by CRLF the next time Git touches it
 scripts/tm-squad-scrape-local.ts | 25 ++++++++++++++++++++++---
 1 file changed, 22 insertions(+), 3 deletions(-)

=== git diff (final hunk) ===
warning: in the working copy of 'scripts/tm-squad-scrape-local.ts', LF will be replaced by CRLF the next time Git touches it
diff --git a/scripts/tm-squad-scrape-local.ts b/scripts/tm-squad-scrape-local.ts
index 3189a0dc..a79a0a7a 100644
--- a/scripts/tm-squad-scrape-local.ts
+++ b/scripts/tm-squad-scrape-local.ts
@@ -205,9 +205,28 @@ async function processClub(
     if (existing.club_id !== club.clubId) {
       stats.players_transfer_detected++;
       if (!allowTransfers) {
-        console.log(
-          `  ⟷ transfer-detected ${existing.last_name} ${e.tmPlayerId}: DB=${existing.club_id} → TM=${club.clubId} (skip; use --allow-transfers)`,
-        );
+        // Slice 144c: TM kennt den Player noch (nur in anderem Club) —
+        // last_squad_check muss gesetzt werden, sonst erscheint der Spieler
+        // spaeter als "nie gescraped" fuer Retired-Detection. Andere Fields
+        // (shirt/mv/club_id) NICHT ueberschreiben — das waere Transfer-Apply
+        // durch die Hintertuer.
+        if (dryRun) {
+          console.log(
+            `  ⟷ transfer-detected ${existing.last_name} ${e.tmPlayerId}: DB=${existing.club_id} → TM=${club.clubId} (dry: would set last_squad_check only)`,
+          );
+          continue;
+        }
+        const { error: transferSkipError } = await supabase
+          .from('players')
+          .update({ last_squad_check: now })
+          .eq('id', existing.id);
+        if (transferSkipError) {
+          console.log(`  ! UPDATE ${existing.last_name} (last_squad_check): ${transferSkipError.message}`);
+        } else {
+          console.log(
+            `  ⟷ transfer-detected ${existing.last_name} ${e.tmPlayerId}: DB=${existing.club_id} → TM=${club.clubId} (metadata skipped; last_squad_check set; use --allow-transfers fuer Full-Apply)`,
+          );
+        }
         continue;
       }
       stats.players_transfer_applied++;

=== Logic-Walkthrough: 4 Pfade (final) ===

Pfad 1 — Non-transfer Player (club_id match):
  Z.205 FALSE → skip transfer-branch
  Z.219+ Build updates {last_squad_check + shirt + mv}
  Z.251 UPDATE alle Fields
  → UNVERAENDERT vs pre-144c

Pfad 2 — Transfer-detected + --allow-transfers:
  Z.205 TRUE + Z.207 FALSE (allowTransfers=true)
  Z.218+ stats.transfer_applied++
  Z.219+ Build updates {last_squad_check + shirt + mv + club_id}
  Z.251 UPDATE alle Fields inkl. club_id
  → UNVERAENDERT vs pre-144c

Pfad 3 — Transfer-detected + !--allow-transfers (dry-run):
  Z.205 TRUE + Z.207 TRUE + dryRun=true
  NEU: Single log-line '(dry: would set last_squad_check only)'
  NEU: continue (kein UPDATE)
  → Kein DB-Write

Pfad 4 — Transfer-detected + !--allow-transfers (prod):
  Z.205 TRUE + Z.207 TRUE + dryRun=false
  NEU: UPDATE {last_squad_check: now} eq(id)
  NEU success: log '(metadata skipped; last_squad_check set; ...)'
  NEU error: log '! UPDATE ... (last_squad_check): <msg>'
  NEU: continue
  → shirt_number/market_value/club_id BLEIBEN intakt
  → last_squad_check wird gesetzt

=== Baseline (Pre-Fix from 144b) ===
  matched=2841  transfer_detected=225  last_squad_check_populated=2616
  2841 - 225 = 2616 EXAKT → bestaetigt early-continue-Bug

=== Post-Fix Erwartung ===
  Beim naechsten Full-Run: last_squad_check_populated == matched
  (nicht hart == 2841 wg. Kader-Drift; math-invariant ist 'matched == populated')
  Counter players_transfer_detected bleibt unveraendert (zaehlt Detection, nicht Commit)
