import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Airdrop | BeScout' };

export default function AirdropLayout({ children }: { children: React.ReactNode }) {
  return children;
}
