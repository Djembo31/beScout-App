/**
 * Shared business constants.
 * Single source of truth for values duplicated between frontend and backend.
 */

/** Total trade fee percentage (must match TRADE_FEE_TOTAL_PCT in backend RPCs) */
export const TRADE_FEE_PCT = 6;
